{
  "name": "@wdtk/source",
  "version": "0.5.0",
  "description": "Development Toolkit",
  "main": "index.js",
  "author": "opfx",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12.13.0"
  },
  "scripts": {
    "build": "yarn workspaces foreach -pt run build",
    "watch": "yarn workspaces foreach -pt run watch",
    "build:compile": "cd \"$0\" && yarn clean",
    "build:clean": "node ./scripts/rimraf  \"$0\"/dist"
  },
  "devDependencies": {
    "@types/node": "14.0.27",
    "prettier": "2.0.5",
    "typescript": "3.9.7"
  }
}
