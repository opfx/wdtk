{
  "$schema": "http://json-schema.org/schema",
  "$id": "WdtkCypressSchematicsInit",
  "title": "Cypress Init Options Schema",
  "type": "object",
  "description": "Adds Cypress support to the workspace.",
  "properties": {
    "skipInstall": {
      "description": "Skip installing after adding @wdtk/cypress",
      "type": "boolean",
      "default": "false"
    }
  },
  "required": []
}
