{
  "$schema": "http://json-schema.org/schema",
  "$id": "WdtkCypressSchematicsProject",
  "title": "Cypress Project Options Schema",
  "type": "object",
  "description": "Generates a Cypress E2E project.",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the frontend project to test."
    }
  },
  "required": []
}
