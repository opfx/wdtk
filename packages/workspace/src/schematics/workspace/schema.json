{
  "$schema": "http://json-schema.org/schema",
  "id": "WdtkWorkspaceSchematicsWorkspace",
  "title": "Creates an empty workspace.",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the new workspace.",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the workspace?"
    },
    "newProjectRoot": {
      "description": "The path where new projects will be created.",
      "type": "string",
      "default": "projects",
      "x-prompt": "What directory should be used for projects?"
    }
  },
  "required": []
}
