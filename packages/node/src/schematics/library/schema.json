{
  "$schema": "http://json-schema.org/schema",
  "id": "WdtkNodeSchematicsLibrary",
  "title": "Node Library Options Schema",
  "type": "object",
  "description": "Generates a new Node library in the workspace.",
  "properties": {
    "name": {
      "description": "The name of the library.",
      "type": "string",
      "format": "html-selector",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the Node library?"
    }
  },
  "required": ["name"]
}
