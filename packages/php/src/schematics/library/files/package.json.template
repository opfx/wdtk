{
  "name": "<%= packageName %>",
  "version": "0.0.1",
  "scripts": {
    "composer": "<%= offsetFromRoot %>/node_modules/.bin/composer",
    "build": "yarn composer run build --no-ansi",
    "test": "yarn composer run test --no-ansi",
    "postinstall": "yarn composer update --no-ansi"
  }
}
