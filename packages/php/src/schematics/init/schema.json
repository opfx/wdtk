{
  "$schema": "http://json-schema.org/schema",
  "id": "WdtkPhpSchematicsInit",
  "title": "Php Init Options Schema",
  "type": "object",
  "description": "Adds PHP support to the workspace.",
  "properties": {
    "skipInstall": {
      "description": "Skip installing dependencies",
      "type": "boolean",
      "default": false
    },
    "skipFormat": {
      "description": "Skip formatting files.",
      "type": "boolean",
      "default": false
    }
  },
  "required": []
}
