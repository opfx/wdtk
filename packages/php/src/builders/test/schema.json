{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "PHP Build Target",
  "description": "PHP test target options for Build Facade.",
  "type": "object",
  "properties": {
    "buildTarget": {
      "type": "string",
      "description": "A build use to build the project, in the format of `project:target[:configuration]`. You can also pass in more than one configuration name as a comma-separated list. Example: `project:target:production,staging`.",
      "pattern": "^[^:\\s]+:[^:\\s]+(:[^\\s]+)?$"
    },
    "maxBatchSize": {
      "type": "integer",
      "description": "Max batch size (only for functional mode).",
      "default": 0
    },
    "parallel": {
      "type": "boolean",
      "description": "Whether to run the tests in parallel or not.",
      "default": true
    },
    "processes": {
      "type": ["integer", "string"],
      "description": "The number of test processes to run.",
      "default": "auto"
    },
    "useFunctionalMode": {
      "type": "boolean",
      "description": "Whether to use the functional mode or not.",
      "default": false
    },
    "useWrapperRunner": {
      "type": "boolean",
      "description": "Whether to use the WrapperRunner or not.",
      "default": true
    }
  },
  "additionalProperties": true
}
