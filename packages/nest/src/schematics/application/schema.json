{
  "$schema": "http://json-schema.org/schema",
  "id": "WdtkNestSchematicsApplication",
  "title": "Nest Application Options Schema",
  "type": "object",
  "description": "Generates a new Nest (NestJS) application in the workspace.",
  "properties": {
    "name": {
      "description": "The name of the application.",
      "type": "string",
      "format": "html-selector",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the Nest application?"
    },
    "directory": {
      "description": "The directory of the new application.",
      "type": "string"
    },
    "linter": {
      "description": "The tool to use for running lint checks.",
      "type": "string",
      "enum": ["eslint", "tslint"],
      "default": "eslint"
    },
    "skipFormat": {
      "description": "Skip formatting files",
      "type": "boolean",
      "default": false
    },
    "skipInstall": {
      "description": "Skip installing after adding @wdtk/nest",
      "type": "boolean",
      "default": false
    },
    "skipPackageJson": {
      "type": "boolean",
      "default": false,
      "description": "Do not add dependencies to package.json."
    },
    "unitTestRunner": {
      "description": "Adds the specified unit test runner",
      "type": "string",
      "enum": ["jest", "none"],
      "default": "jest"
    },
    "babelJest": {
      "type": "boolean",
      "description": "Use babel instead ts-jest",
      "default": false
    }
  },
  "required": ["name"]
}
