{
  "$schema": "http://json-schema.org/schema",
  "$id": "wx-cli://commands/new/schema.json",
  "description": "Creates a new workspace.",
  "$longDescription": "./long-description.md",
  "$aliases": ["n"],
  "$scope": "out",
  "$impl": "./index#NewCommand",
  "$type": "schematic",
  "type": "object",
  "allOf": [
    {
      "properties": {
        "collection": {
          "type": "string",
          "aliases": ["c"],
          "description": "A collection of schematics to use in generating the workspace."
        },
        "verbose": {
          "type": "boolean",
          "default": false,
          "aliases": ["v"],
          "description": "When true, adds more details to output logging."
        }
      }
    },
    { "$ref": "./../definitions.json#/definitions/base" },
    { "$ref": "./../definitions.json#/definitions/schematic" },
    { "$ref": "./../definitions.json#/definitions/interactive" }
  ]
}
